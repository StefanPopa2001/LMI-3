# Production Environment Variables
# API URL for production (same domain, routed through Traefik)
NEXT_PUBLIC_API_URL=https://popa-stefan.be/LMI3/API

# Environment
NODE_ENV=production
COMPOSE_BAKE=true

# Debug / relaxed validation flags (mirrors backend; keep false in real production)
NEXT_PUBLIC_DEBUG_NO_VALIDATION=true
NEXT_PUBLIC_DEFAULT_PASSWORD=dhkak81

# Build metadata (overwritten at container runtime via docker-compose)
NEXT_PUBLIC_BUILD_ID=local-prod