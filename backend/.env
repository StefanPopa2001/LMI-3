# Environment varia# ======================= MAILJET API CONFIG =======================
MJ_APIKEY_PUBLIC=93366d97d7776c1abb409e93ed34e960
MJ_APIKEY_PRIVATE=7d0e6b560a5bb4333b17d9aafe020d66
MAIL_FROM="LMI 3 APP <>"  # Must be verified in Mailjet in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Database connection - CHANGE CREDENTIALS IN PRODUCTION!
DATABASE_URL=postgresql://user:password@db:5432/mydb

# Redis (optional, for caching)
REDIS_URL=redis://redis:6379

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
SECRET_KEY="ZeerbaidjanUberAlles"

# Server configuration
NODE_ENV=production
PORT=4000
COMPOSE_BAKE=true
# ======================= DEBUG / RELAXED VALIDATION (TEMP) =======================
# Enable to bypass most input validation & sanitization and force a known default password for new/reset users.
# WARNING: This is INSECURE and must NOT be enabled in real production environments exposed to users.
DEBUG_NO_VALIDATION=true
DEFAULT_PASSWORD=dhkak81

# Expose to frontend (Next.js) via public env vars for consistent UX & banners.
NEXT_PUBLIC_DEBUG_NO_VALIDATION=true
NEXT_PUBLIC_DEFAULT_PASSWORD=dhkak81
# Frontend API URL is set via docker-compose for production (e.g. https://popa-stefan.be/api)
# Leave NEXT_PUBLIC_API_URL empty here to avoid accidental localhost override in container.
# NEXT_PUBLIC_API_URL=

# ======================= SMTP / EMAIL CONFIG (optional) =======================
# If these are NOT set, backend creates a temporary Ethereal test account and returns preview URLs.
# To use a real SMTP server, uncomment and fill:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=yourgmail@gmail.com
# SMTP_PASS=your_app_password_here
# MAIL_FROM="LMI 3 APP"
